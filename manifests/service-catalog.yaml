# Service catalog has been moved to a secure location.
# See: ~/legacy-credentials/service-catalog.yaml
#
# This file was moved because it contains spend data, console URLs,
# and production endpoints that no agent needs unless explicitly doing infra work.
